@model ChatViewModel
@{
    var chat = Model;
    ViewData["Title"] = "Чаты";
}

<div class="chat-container">
    <!-- Список чатов -->
    <aside class="chat-sidebar">
        <h2>Чаты</h2>
        @if (chat.AnotherChats != null && chat.AnotherChats.Count != 0)
        {
            <ul class="chat-list">
                @foreach (var element in chat.AnotherChats)
                {
                    <li class="chat-item online">
                        <span class="name">@element.ChatName</span>
                        <span class="status-indicator"></span>
                    </li>
                }
            </ul>
        }
    </aside>

    <!-- Окно чата -->
    <section class="chat-window">
        <header class="chat-header">
            <img src="/images/avatar1.jpg" alt="User" class="avatar" />
            <h2>Андрей</h2>
        </header>

        <div class="chat-messages">
            <div class="message received">
                <p>Привет! Как дела?</p>
                <span class="time">12:45</span>
            </div>
            <div class="message sent">
                <p>Привет! Все отлично, а у тебя?</p>
                <span class="time">12:46</span>
            </div>
        </div>

        <footer class="chat-input">
            <input type="text" placeholder="Введите сообщение..." />
            <label for="fileInput" class="attach-btn">📎</label>
            <input type="file" id="fileInput" style="display: none;" />
            <button class="send-btn">➤</button>
        </footer>
    </section>
</div>

@if (Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
{
    <link rel="stylesheet" href="~/Styles/ChatCard.css">
}
else
{
    <link rel="stylesheet" href="~/Styles/ChatCard.min.css?v = @DateTime.Now.Ticks">
}